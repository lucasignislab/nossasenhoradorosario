/**
 * LAYOUT DA PÁGINA DE SENHAS NO DASHBOARD
 * Copie para: src/app/(auth)/dashboard/senhas/layout.tsx
 */

export default function SenhasLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
